import { JSX } from "react";
import { TagProps } from './Tag.props';
import styles from "./Tag.module.css";
// import cn from "classnames";

export const Tag = ({ appearence, children }: TagProps): JSX.Element => {
    return (
       <></>
    );
};
